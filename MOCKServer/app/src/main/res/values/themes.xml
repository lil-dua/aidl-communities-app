<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MOCKServer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/secondary_color</item>
    </style>

    <style name="Theme.MOCKServer" parent="Base.Theme.MOCKServer" />
</resources>